import { CalendarDataQuery } from './calendar-data-query';

describe('CalendarDataQuery', () => {
  it('should create an instance', () => {
    expect(new CalendarDataQuery()).toBeTruthy();
  });
});
